{% extends 'base.html.twig' %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <a  href="{{ path('appSendEmailToContact') }}"> Envoyer un nouveau mail</a>
    <table border="3">
        <thead>
        <tr>
            <td>
                Date:
                
            </td>
            <td>
                Objet:
            </td>
            <td>
                Message:
            </td>
            <td>
                Destinataire:
            </td>
            <td>
                action:
            </td>
        </tr>

        </thead>
        <tbody>
        {% for email in emails %}

        <tr>
        <td>
                {{ email.DateEnvoie| date('d/m/Y') }}

            </td>
           
            <td>
                {{ email.objet }}

            </td>
             <td>
                {{ email.message }}

            </td>
            <td>
                {% for destinataire in email.destinataire %}
                    {{ destinataire.email }}{% if not loop.last %}, {% endif %}
                {% endfor %}
            </td>
            <td>
            <td>
            <a href="{{ path('appMailContactdelete', {'id':email.id}) }}"> Supprimer</a>
            </td>
            </td>
        </tr>
        {% endfor %}

        </tbody>
    </table>
    <ul>
    </ul>
</div>
{% endblock %}
