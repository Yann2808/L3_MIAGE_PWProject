{% extends 'base.html.twig' %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <a  href="{{ path('sendEmailToEdu') }}"> Envoyer un nouveau mail</a>
    <table border="3">
        <thead>
        <tr>
            <td>
                Date:
                
            </td>
            <td>
                Objet:
            </td>
            <td>
                Message:
            </td>
            <td>
                Destinataire:
            </td>
            <td>
                action:
            </td>
        </tr>

        </thead>
        <tbody>
        {% for email in emails %}

        <tr>
        <td>
                {{ email.DateEnvoi| date('d/m/Y') }}

            </td>
           
            <td>
                {{ email.objet }}

            </td>
             <td>
                {{ email.message }}

            </td>
            <td>
                {% for expediteur in email.expediteur %}
                    {{ expediteur.email }}
                {% endfor %}
            </td>
            <td>
            <td>
            <a href="{{ path('MailEdudelete', {'id':email.id}) }}"> Supprimer</a>
            </td>
            </td>
        </tr>
        {% endfor %}

        </tbody>
    </table>
    <ul>
    </ul>
</div>
{% endblock %}
